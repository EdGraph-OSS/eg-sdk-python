name: Publish Package

on:
  push:
    branches: [ main ]

permissions:
  contents: read
  packages: write

jobs: 
  publish-package: 
    runs-on: ubuntu-latest

    steps: 
      - name: ⚙️ Checkout Repository
        uses: actions/checkout@v4
        with: 
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.8"

      - name: Install build tools
        run: |
          python -m pip install --upgrade pip build twine

      - name: Build distribution
        run: python -m build
        
      - name: 🌐 Publish Package 
        env:
          TWINE_USERNAME: ${{ github.actor }}
          TWINE_PASSWORD: ${{ secrets.GITHUB_TOKEN }}
          TWINE_REPOSITORY_URL: https://nuget.pkg.github.com/${{ github.repository_owner }}/index.json
        run: |
          python -m twine upload --repository-url https://github.com/orgs/${{ github.repository_owner }}/packages/pypi dist/*
          

